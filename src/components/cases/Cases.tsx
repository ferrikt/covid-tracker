import * as React from 'react';
import * as Styled from './Styled'

const Cases: React.SFC<{}> = () => {
  return (
    <Styled.CasesWrapper>
      <h1>Cases</h1>
    </Styled.CasesWrapper>
  )
}

export default Cases;