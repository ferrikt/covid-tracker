import * as React from 'react';
import { Grid, Flex } from 'theme-ui';
import News from './News';

interface IProps {}

const Map: React.SFC<IProps> = (props: IProps) => <>Its a map</>;

export default Map;
